#include<bits/stdc++.h> // HEADER FILE
    
using namespace std;
    
int main() // MAIN DEFINATION
{
    int n;
    cout << "Enter the Number : ";
    cin >> n;

    string s = to_string(n);

    int size = s.size();

    

    return 0;
}