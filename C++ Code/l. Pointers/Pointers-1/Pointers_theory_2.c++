#include<bits/stdc++.h> // HEADER FILE
    
using namespace std;
    
int main() // MAIN DEFINATION
{
    ios_base::sync_with_stdio(false);  //For fast I/O
    cin.tie(0);
    cout.tie(0);

    int *ptr; // Only declaring the pointer

    cout << ptr << endl;

    int marks = 90;

    ptr = &marks; // re-assignment / updating the pointer with some data

    cout << ptr;
    return 0;
}