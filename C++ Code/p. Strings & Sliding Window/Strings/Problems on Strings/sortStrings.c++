#include <bits/stdc++.h> // HEADER FILE

using namespace std;

int main() // MAIN DEFINATION
{
    ios_base::sync_with_stdio(false); // For fast I/O
    cin.tie(0);
    cout.tie(0); 
    string str1 = "codingwallah";
    sort(str1.begin(), str1.end());
    cout << str1 << endl;

    string str2 = "star";
    sort(str2.begin(), str2.end());
    cout << str2 << endl;
    return 0;
}