#include <bits/stdc++.h> // HEADER FILE

using namespace std;

int main() // MAIN DEFINATION
{
    int total, boys, girls;
    boys = 17;
    total = (80 * 45) / 100;
    girls = total - boys;
    cout << "Number of Girls: " << girls << endl;
    return 0;
}