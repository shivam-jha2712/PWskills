#include<bits/stdc++.h> // HEADER FILE
    
using namespace std;
    
int main() // MAIN DEFINATION
{
    
    int length, breadth;
    cin >> length >> breadth;

    cout << "Area of the rect is :" << length*breadth;

    return 0;
}