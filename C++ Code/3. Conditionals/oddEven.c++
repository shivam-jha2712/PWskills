#include <bits/stdc++.h> // HEADER FILE

using namespace std;

int main() // MAIN DEFINATION
{
    ios_base::sync_with_stdio(false); // For fast I/O
    cin.tie(0);
    cout.tie(0);

    int a;
    cin >> a;
    if (a % 2 == 0)
        cout << "even" << endl;
    else
        cout << "odd" << endl;

    return 0;
}