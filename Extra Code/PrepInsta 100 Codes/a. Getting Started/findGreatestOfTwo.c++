#include <bits/stdc++.h> // HEADER FILE

using namespace std;

int main() // MAIN DEFINATION
{
    int n, m;
    cout << "Enter the numbers: ";
    cin >> n >> m;

    cout << max(n, m);
    return 0;
}