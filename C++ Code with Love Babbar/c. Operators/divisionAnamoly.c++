#include <bits/stdc++.h> // HEADER FILE

using namespace std;

int main() // MAIN DEFINATION
{
    ios_base::sync_with_stdio(false); // For fast I/O
    cin.tie(0);
    cout.tie(0);

    int a, b;
    float c = 2.0/5;

    // cout << "Take input as 2 5 2.0 to see the diffrence: " << endl;

    cin >> a >> b ;

    cout << a/b << " " << c << endl;
    return 0;
}