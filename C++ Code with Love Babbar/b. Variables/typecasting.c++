#include <bits/stdc++.h> // HEADER FILE

using namespace std;

int main() // MAIN DEFINATION
{
    ios_base::sync_with_stdio(false); // For fast I/O
    cin.tie(0);
    cout.tie(0);

    int a = 'a';
    cout << a << '\n';
    // Type casting the character to it corresponding integer(ASCII) value.

    // a - z == 97 - 122
    // A - Z == 65 - 90
    // 0 - 9 == 48 - 57

    char ch = 97;
    cout << ch << endl;

    return 0;
}