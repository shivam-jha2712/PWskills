#include <bits/stdc++.h> // HEADER FILE

using namespace std;

int main() // MAIN DEFINATION
{
    ios_base::sync_with_stdio(false); // For fast I/O
    cin.tie(0);
    cout.tie(0);

    // for getting the ASCII value for the entered character value
    char c;
    cin >> c;
    cout << (int)c << endl;

    return 0;
}